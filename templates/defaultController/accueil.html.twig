{% extends "base.html.twig" %}
{% block title %} {{ parent() }} Accueil {% endblock %} {% block content %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-12 col-md-6 align-items-center">
            <h1 class="text-center text-primary fw-bold m-2">Accueil</h1>
            {% for type in types %} 
            {{type.id}} 
            <a href="index.php?page=ProductListebyType&type_id={{type.id}}" style="color: #000">{{ type.label}}</a>
            <a href="index.php?page=updateType&id={{type.id}}"><i class="bi bi-pen-fill"></i></a>
            {% endfor%}
        </div>
    </div>
</div>
{% endblock %} 